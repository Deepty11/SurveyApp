<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rehnuma.surveyapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewSurveyActivity"></activity>
        <activity android:name=".FinalActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".TakeSurvey5Activity" />
        <activity android:name=".TakeSurvey4Activity" />
        <activity android:name=".TakeSurvey3Activity" />
        <activity android:name=".TakeSurvey2Activity" />
        <activity android:name=".TakeSurvey1Activity" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>